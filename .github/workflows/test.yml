name: test

on:
  push:
  pull_request:

jobs:
  test:
    runs-on: macos-14
    steps:
      - uses: actions/checkout@v7.0.1
      - uses: astral-sh/setup-uv@v10.1.0
        with:
          enable-cache: true
      - run: brew install ffmpeg
      - run: uv sync --locked
      - run: uv run --locked python -m unittest discover -s tests
      - run: uv run --locked vibevoice-transcribe --self-check
